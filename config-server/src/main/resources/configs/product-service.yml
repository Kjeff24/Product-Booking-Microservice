spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/product-db

server:
  port: 0
# Generate a unique name for the instance
eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

app:
  prop1: publish and subscribe using rabbitMQ testing 2

